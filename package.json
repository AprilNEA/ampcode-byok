{
	"name": "ampcode-byok",
	"type": "module",
	"cloudflare": {
		"bindings": {
			"SHARED_SECRET": {
				"description": "Cloudflare [Secrets Store](https://developers.cloudflare.com/secrets-store/) entry holding the secret the local `amp` CLI sends as a Bearer token. Generate the value with `openssl rand -hex 32`. The Deploy button lets you pick an existing entry or create a new one."
			}
		}
	},
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy --minify",
		"typecheck": "tsc --noEmit",
		"cf-typegen": "wrangler types --env-interface Bindings"
	},
	"dependencies": {
		"hono": "^4.12.18"
	},
	"devDependencies": {
		"@types/node": "^25.7.0",
		"typescript": "^6.0.3",
		"wrangler": "^4.90.1"
	}
}