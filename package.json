{
	"name": "ampcode-byok",
	"type": "module",
	"cloudflare": {
		"bindings": {
			"SHARED_SECRET": {
				"description": "The shared secret the local `amp` CLI must send as a Bearer token. Generate a strong value with `openssl rand -hex 32` and paste it here. You can rotate it later with `wrangler secret put SHARED_SECRET`."
			}
		}
	},
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy --minify",
		"typecheck": "tsc --noEmit",
		"cf-typegen": "wrangler types --env-interface Bindings"
	},
	"dependencies": {
		"hono": "^4.12.18"
	},
	"devDependencies": {
		"@types/node": "^25.7.0",
		"typescript": "^6.0.3",
		"wrangler": "^4.90.1"
	}
}